import express from 'express'; import {markPickup,markDrop,markArrival,markDeparture,todayAttendance,studentAttendance,routeAttendance,dailyReport} from '../controllers/attendanceController.js'; import {protect,roleOnly} from '../middleware/authMiddleware.js';
const r=express.Router(); r.post('/pickup',protect,roleOnly('driver','admin'),markPickup); r.post('/drop',protect,roleOnly('driver','admin'),markDrop); r.post('/arrival',protect,roleOnly('driver','admin'),markArrival); r.post('/departure',protect,roleOnly('driver','admin'),markDeparture); r.get('/today',protect,todayAttendance); r.get('/student/:studentId',protect,studentAttendance); r.get('/route/:routeId',protect,routeAttendance); r.get('/report/daily',protect,roleOnly('driver','admin'),dailyReport); export default r;

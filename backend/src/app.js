import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import morgan from 'morgan'; import rateLimit from 'express-rate-limit'; import authRoutes from './routes/authRoutes.js'; import studentRoutes from './routes/studentRoutes.js'; import routeRoutes from './routes/routeRoutes.js'; import attendanceRoutes from './routes/attendanceRoutes.js'; import delayRoutes from './routes/delayRoutes.js'; import dashboardRoutes from './routes/dashboardRoutes.js'; import reportRoutes from './routes/reportRoutes.js'; import {notFound,errorHandler} from './middleware/errorMiddleware.js';
const app=express(); app.use(helmet()); app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173',credentials:true})); app.use(express.json({limit:'1mb'})); app.use(morgan('dev')); app.use(rateLimit({windowMs:15*60*1000,limit:300})); app.get('/',(req,res)=>res.json({message:'SafeRide Ledger API is running'})); app.use('/api/auth',authRoutes); app.use('/api/students',studentRoutes); app.use('/api/routes',routeRoutes); app.use('/api/attendance',attendanceRoutes); app.use('/api/delays',delayRoutes); app.use('/api/reports',reportRoutes); app.use('/api',dashboardRoutes); app.use(notFound); app.use(errorHandler); export default app;

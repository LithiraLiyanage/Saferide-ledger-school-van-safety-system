import {useEffect,useState} from 'react';import api from '../../services/api';import {DashboardCard,LoadingSpinner,ChartCard} from '../../components/UI';import {AlertTriangle,Bus,Shield,Users} from 'lucide-react';import {Bar,BarChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';export default function AdminDashboard(){const[data,setData]=useState(null);useEffect(()=>{api.get('/admin/dashboard').then(r=>setData(r.data))},[]);if(!data)return <LoadingSpinner/>;const chart=[{name:'Pickup %',value:data.pickupCompletion},{name:'Drop %',value:data.dropCompletion},{name:'Safety',value:data.safetyScore}];return <div><h1 className="mb-6 text-4xl font-black">🛡️ Admin Dashboard</h1><div className="grid gap-5 md:grid-cols-4"><DashboardCard title="Students" value={data.totalStudents} icon={<Users/>} tone="dark"/><DashboardCard title="Parents" value={data.totalParents} icon={<Users/>}/><DashboardCard title="Drivers" value={data.totalDrivers} icon={<Bus/>}/><DashboardCard title="Active Routes" value={data.activeRoutes} icon={<Bus/>}/><DashboardCard title="Pickup Completion" value={`${data.pickupCompletion}%`} icon={<Shield/>}/><DashboardCard title="Drop Completion" value={`${data.dropCompletion}%`} icon={<Shield/>}/><DashboardCard title="Active Delays" value={data.activeDelays} icon={<AlertTriangle/>}/><DashboardCard title="Safety Score" value={`${data.safetyScore}%`} icon={<Shield/>} tone="dark"/></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><ChartCard title="Safety Performance"><ResponsiveContainer width="100%" height={300}><BarChart data={chart}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#F59E0B"/></BarChart></ResponsiveContainer></ChartCard><div className="card"><h2 className="mb-4 text-xl font-black">Recent Safety Logs</h2>{data.logs?.map(l=><div key={l._id} className="border-t py-3 text-sm"><b>{l.action}</b> · {l.description}</div>)}</div></div></div>}

import {useEffect,useState} from 'react';import api from '../../services/api';import {DashboardCard,LoadingSpinner,ChartCard} from '../../components/UI';import SafetyTimeline from '../../components/SafetyTimeline';import {StatusBadge} from '../../components/Badges';import {Bell,CheckCircle2,Clock,Shield} from 'lucide-react';import {Bar,BarChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';export default function ParentDashboard(){const[data,setData]=useState(null);useEffect(()=>{api.get('/parent/dashboard').then(r=>setData(r.data))},[]);if(!data)return <LoadingSpinner/>;const main=data.attendance?.[0];const chart=[{name:'Picked',value:data.attendance.filter(a=>a.pickupStatus==='picked_up').length},{name:'Dropped',value:data.attendance.filter(a=>a.dropStatus==='dropped').length},{name:'Absent',value:data.attendance.filter(a=>a.pickupStatus==='absent').length}];return <div><h1 className="mb-6 text-4xl font-black">🛡️ Parent Dashboard</h1><div className="mb-6 rounded-[2rem] bg-gradient-to-r from-slate-950 to-slate-800 p-8 text-white shadow-glow"><p className="font-black text-amber-300">Parent Peace-of-Mind Card</p><h2 className="mt-2 text-4xl font-black">Your child is currently: <span className="text-amber-400">{main?.overallStatus?main.overallStatus.replace('_',' '):'Pending'}</span></h2></div><div className="grid gap-5 md:grid-cols-4"><DashboardCard title="Children" value={data.students.length} icon={<Shield/>} tone="dark"/><DashboardCard title="Today Records" value={data.attendance.length} icon={<CheckCircle2/>}/><DashboardCard title="Active Delays" value={data.delays.length} icon={<Clock/>}/><DashboardCard title="Notifications" value={data.notifications.length} icon={<Bell/>}/></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><SafetyTimeline attendance={main||{}}/><ChartCard title="Today Attendance Summary"><ResponsiveContainer width="100%" height={260}><BarChart data={chart}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#F59E0B"/></BarChart></ResponsiveContainer></ChartCard></div><h2 className="mb-4 mt-8 text-2xl font-black">🚨 Active Delay Alerts</h2><div className="space-y-3">{data.delays.map(d=><div key={d._id} className="card"><StatusBadge status={d.status}/><h3 className="mt-3 font-black">{d.route?.routeName}</h3><p>{d.message} · {d.estimatedDelayMinutes} minutes</p></div>)}</div></div>}

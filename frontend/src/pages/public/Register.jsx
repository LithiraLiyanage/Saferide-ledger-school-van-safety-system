import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {toast} from 'react-toastify';import {useAuth} from '../../context/AuthContext';export default function Register(){const[form,setForm]=useState({name:'',email:'',phone:'',password:'',confirm:''});const{register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();if(form.name.length<2)return toast.error('Name must be at least 2 characters');if(form.password!==form.confirm)return toast.error('Passwords do not match');await register({name:form.name,email:form.email,phone:form.phone,password:form.password});nav('/app')};return <div className="flex min-h-screen items-center justify-center bg-amber-50 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-[2rem] bg-white p-8 shadow-2xl"><h1 className="text-3xl font-black">Create account</h1>{['name','email','phone','password','confirm'].map(k=><input key={k} className="input mt-4" type={k==='password'||k==='confirm'?'password':'text'} placeholder={k==='confirm'?'Confirm password':k} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>) }<button className="btn-primary mt-6 w-full">Register</button><p className="mt-5 text-center text-sm">Already have account? <Link className="font-black text-amber-600" to="/login">Login</Link></p></form></div>}

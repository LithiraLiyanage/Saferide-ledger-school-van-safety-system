import {CheckCircle2,Circle} from 'lucide-react';export default function SafetyTimeline({attendance}){const steps=[['Home Pickup',attendance?.pickupStatus==='picked_up'||attendance?.pickupStatus==='absent'],['In Van',attendance?.overallStatus==='in_transit'||attendance?.schoolArrivalStatus==='arrived'||attendance?.dropStatus==='dropped'],['Arrived School',attendance?.schoolArrivalStatus==='arrived'],['Left School',attendance?.departureStatus==='departed'],['Dropped Home',attendance?.dropStatus==='dropped']];return <div className="card"><h3 className="mb-5 text-xl font-black">🛡️ Safety Timeline</h3><div className="space-y-4">{steps.map(([n,d])=><div key={n} className="flex items-center gap-3">{d?<CheckCircle2 className="text-green-600"/>:<Circle className="text-slate-300"/>}<span className={d?'font-black text-slate-950':'font-bold text-slate-400'}>{n}</span></div>)}</div></div>}

import {Navigate,Outlet} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {LoadingSpinner} from './UI';export function ProtectedRoute(){const{user,loading}=useAuth();if(loading)return <LoadingSpinner/>;return user?<Outlet/>:<Navigate to="/login"/>}export function RoleRoute({roles}){const{user,loading}=useAuth();if(loading)return <LoadingSpinner/>;return roles.includes(user?.role)?<Outlet/>:<Navigate to={user?.role==='admin'?'/app/admin':user?.role==='driver'?'/app/driver':'/app/parent'}/>}
